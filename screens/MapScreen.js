import React from 'react'
import { View, Text } from 'react-native'

const MapScreen = () => {
    return (
        <View>
            <Text>Here is the map ...</Text>
            <Text> To load the map we may need the GCP cloud which we currently dont have</Text>
        </View>
    )
}

export default MapScreen
